import test from 'node:test';
import assert from 'node:assert/strict';
import {estimateCost} from '../src/cost.js';
test('cost is unknown without explicit complete billing inputs; cached attribution and actual usage stay separate',()=>{const table={schema_version:'geap-price-table.v1',version:'test-only.v1',currency:'TEST',source:'synthetic-unit-test',confirmed_at:'2026-09-07',models:[{model_id:'test',coverage_confirmed:true,components:[{usage_field:'promptTokenCount',unit:1000,price:2,description:'synthetic test price, not a real model price'}]}]},a={attempt_id:'a',model_id:'test',usageMetadata:{promptTokenCount:100}};assert.equal(estimateCost([a],null).total,null);assert.equal(estimateCost([a],table,{mock:true}).total,null);assert.equal(estimateCost([{...a,usageMetadata:null}],table).total,null);const c=estimateCost([a],table,{attributedStage1:[{...a,attempt_id:'cached'}]});assert.equal(c.actual_spend.total,.2);assert.equal(c.standalone_attribution.total,.4);assert.equal(c.cost_status,'estimated');});
