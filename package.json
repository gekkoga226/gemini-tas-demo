{
  "name": "gemini-tas-local-mock",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "probe": "node tools/geap-probe/probe.mjs",
    "probe:self-test": "node tools/geap-probe/self-check.mjs"
  },
  "engines": {
    "node": ">=20.11"
  }
}
