# Few-shot TAS 引き継ぎ（2026-09-07）

## 再開後の完了記録（2026-09-07 21:04 JST）

以下の停止時スナップショットに記載された第4節の残作業は完了した。最終全テスト記録は34/34成功（失敗0）。最新サーバーで前回中断履歴からの再試行成功に加え、画面から再判定を中止→中断表示→履歴から再試行→成功を確認した。Stage1共有を維持、一時媒体は削除完了、画面とコンソール確認済み。`git diff --check` 成功、凍結プロンプトのSHA-256一致。詳細と変更一覧は `docs/FEWSHOT_IMPLEMENTATION.md` に反映した。

今回の再開でコード修正は不要、編集は本書と実装記録のみ。ローカルモックの完成確認は終了し、現在依頼された範囲の未完了タスクはない。実API・精度・性能・費用・実測Checkは未実施のまま。追加の実測は別途指示と必要な入力・認証等が揃ってから行う。コミット・push・公開・デプロイなし。検証サーバーは最新コードで `http://127.0.0.1:4173` に起動中。

以下は再開前の状態を残した記録であり、「未完了」「次に実施」の記述より本節を優先する。

## 1. 現在の最終目的
Round 18 を正として、既存デモをローカルで一通り操作できる明示的モックに改修し、実接続アダプター・Check 7〜11・必要なテスト・ブラウザ検証・運用文書まで完成させる。作業場所は `C:/Users/mizuk/work/gemini-tas-demo`。ユーザー指示で現在停止中。この引き継ぎ作成後の実装・調査・テストは行っていない。

## 2. 完了したこと
- 3方式 × Zero/Few の独立選択、共通 Observation、固定／発見スキーマと派生プロンプト、Stage2 の候補・根拠・生確信度・低下理由・確認対象判定を実装。
- 案1/3の同一 Stage1 artifact 共有、案2の分離、入力・版・ハッシュ・共有元の保存、キャッシュ検証、明示的移行、Stage2 再実行を実装。
- 標準セット作成・代表画像抽出・出自保存・確認公開・廃止、GT隔離、語彙の手動登録／PDF抽出と確認を実装。
- 永続ジョブ、切断継続、中止、失敗再試行、再起動回復、一時媒体の削除台帳・猶予・再試行を実装。
- 既存デザインに沿う上下タイムライン、区間同期／独立再生、警告・根拠確認、修正・Undo/Redo・保存・履歴・原本／修正版JSON出力を実装。
- Gemini/GEAP接続、認証・再試行・GCS媒体処理、未確認時に金額を捏造しない費用計算、Check 7〜11 とオフライン採点を実装。既存 Check 0〜6 は維持、Check 2 は参考測定。
- README、運用文書、仕様整合文書、D1〜D10対応表を更新。
- ブラウザで全6条件、標準公開、動画登録、候補・警告、修正保存、履歴復元、両JSON出力、再読込中の処理継続、同期／独立再生を確認。

## 3. 現在実装途中のこと
機能追加は概ね完了。最終検証結果の回収、最新コードでの起動・画面確認、実装記録の最終更新と差分監査が未完了。最後のブラウザ操作は visual_evidence / Zero-shot の Stage2 再実行を中止したところで、中止状態の表示と画面からの再試行はまだ確認していない。

## 4. 次に実施すべき作業（優先順）
1. ユーザーの再開指示後、本書と実装記録を読み、Git状態を確認して既存変更を保全する。
2. 停止指示直前に開始済みの最終全テスト結果を確認する。出力先は `.local-validation/final-tests.txt`。開始時点では最初の2件が成功、最終集計は未確認。旧タスクの実行セッションIDは55824で、新タスクでは利用できない可能性あり。
3. 必要ならローカルサーバーを再起動して最新コードを反映する。旧サーバーは `http://127.0.0.1:4173`、実行セッション8601。起動設定は PORT=4173、ENABLE_MOCK_FAULTS=true。最終の価格表示・版登録・媒体バイト数などの修正前に起動したプロセスなので、現時点では最新バックエンドではない。
4. ブラウザで中止状態→履歴から再試行→成功を確認し、最新コードで短い最終スモークと画面／コンソール確認を行う。既に確認済みの全操作を無理由にやり直す必要はない。
5. `docs/FEWSHOT_IMPLEMENTATION.md` の「最終確認を継続中」を実際の結果へ更新。全6条件、アップロード、独立再生、両JSON、最後の中止／再試行、最終テスト集計を反映する。変更ファイル一覧もまとめる。
6. 差分整合・凍結ファイル不変を確認し、モック完成と実測未実施を区別して最終報告する。コミット・push・外部公開・デプロイ不要。既存goalは未完了で、新規作成しない。

## 5. 変更済み・重要なファイルと役割
| ファイル | 役割 |
|---|---|
| `server.js`, `src/routes.js` | ローカル起動、認可、ジョブAPI、媒体配信、保存原本のJSONダウンロード |
| `src/pipeline.js` | 標準セット／分析／再実行／公開／修正／回復／キャッシュの中心 |
| `src/contracts.js`, `src/response-schemas.js`, `src/core.js` | 結果契約、時刻不変、検証、正規化・ハッシュ |
| `src/store.js`, `src/cleanup.js` | 永続化・単一ワーカー・削除台帳・競合制御 |
| `src/settings.js`, `src/geap.js`, `src/media.js`, `src/cost.js` | 設定、実接続、動画／画像／PDF処理、確認済み単価による費用計算 |
| `src/mock-model.js` | 新しい結果契約に沿う明示的合成結果 |
| `public/index.html`, `public/styles.css`, `public/app.js`, `public/fewshot.js` | 操作画面と結果表示アダプター |
| `public/segments.js` | 既存処理と新規警告の集約先 |
| `prompts/*.md`, `prompts/round18.v1.json` | 固定／発見等の派生プロンプトとハッシュ版管理 |
| `tools/prepare-prompt-manifest.mjs` | プロンプト版マニフェスト作成 |
| `tools/geap-probe/probe.mjs`, `extra-checks.mjs`, `scoring.mjs` | Check 0〜11 の実行、入力凍結、採点 |
| `tools/geap-probe/cleanup.mjs`, `self-check.mjs` | プローブ媒体の回収、外部API不要の検証報告 |
| `test/{fewshot,scoring,geap-adapter,failure-paths,assets,evaluation,cost}.test.js`, `test-support/fixture.js` | 追加契約・異常経路・採点テスト |
| `README.md`, `tools/geap-probe/README.md`, `docs/FEWSHOT_OPERATIONS.md` | 起動・操作・接続設定・プローブ手順 |
| `docs/FEWSHOT_IMPLEMENTATION.md` | 着手順例外、D1〜D10対応、検証・残作業記録 |
| `docs/SPEC.md`, `docs/DESIGN.md`, `docs/IMPLEMENTATION_RESULT.md` | 現行仕様への導線と旧仕様の履歴扱い |
| `package.json`, `.gitignore` | 実行コマンド・生成データ除外 |

## 6. 実装上すでに決定した重要事項
- Round 18 優先。12.9の実測ゲートは実運用／精度合格判定として維持し、ユーザー承認済みの着手順例外によりモック・接続コードを先行実装した。
- GEAPは Gemini Enterprise Agent Platform。APIはv1beta1、mediaProcessingはPart直下。GEAP Files APIなし、動画はGCS URI、バケットを読む主体はサービスエージェント。
- 通常推論は1リクエスト1動画。案3は実作業動画と標準JPEGを使用し、標準フル動画を同時投入しない。
- Stage2 は全方式で区間時刻を変更できない。GT原本と匿名のお手本ビューを分離し、実作業GTは採点専用。
- モック／合成媒体／疑似使用量を明示。実接続失敗のモック置換は禁止。価格未確認／使用量欠落なら金額は不明。
- 凍結 `reference/validated_prompt.md` は変更していない。SHA-256: `06bfefc4e758e54bf8b4b54714fa727e4be68cc6176bf529402fce69a9ba8db5`。
- 中止と結果公開を直列化し、遅着応答は生応答のみ保存して成功結果を公開しない。媒体再利用と削除も直列化。
- Cloud Run必須ではなく、今回はデプロイしない。ローカル実接続はgcloud認証を利用可能。

## 7. 既知の問題・エラー
- 最終全テストの集計は未回収。それ以前の全テストは34/34成功。未解消の既知テスト失敗はない。
- 実API・モデルID・APIスキーマ互換性・精度・20分性能・費用は未実測。全Checkの実測合格は未判定で、ローカル成功を実測PASSにしていない。
- CUAのファイル選択／ダウンロード通知取得で非常に長い待ち時間が生じたことがある。最終的に登録・ダウンロードは成功。必要以上にイベント待ちを繰り返さない。
- アップロード途中のローカル部分ファイルは直接削除／起動時回収であり、削除台帳統合ではない。変換媒体とGCS媒体は台帳管理済み。
- GCSアップロード自体の401再認証は未実装。Gemini生成リクエストの401再認証は実装・テスト済み。
- 実接続のモデル版スコープは未指定時に起動ごとに変わるため、公開標準の再利用には固定 `MODEL_REVISION_SCOPE` 設定が必要（文書化済み）。
- 初期デモ標準画像に古い丸めの影響で1.30/4.90秒の抽出記録がある。新規抽出は小数正規化済み。既存出自記録は改ざんせず保持。
- プローブの品質合否は人の判定待ちを未確認として保持する。自動で実運用合格を宣言する構成ではない。

## 8. 実行済みテストと結果
- 既存18件は維持。追加を含む全34件成功を複数回確認。その後の最終全実行のみ集計未確認。
- 検証範囲：全6条件、Stage1共有／分離、GT隔離、時刻不変、根拠・確信度、標準公開／移行、修正、失敗・再試行・中止遅着・再起動、切断継続、削除失敗／猶予／再利用、認証・API再試行、価格不明、採点算法。
- Check 7〜11 合成統合テストは51レコードと各3反復、共有artifact、入力凍結、Check 8/10のAPI不要再採点を検証。
- `npm run probe:self-test` 成功。最新明示実行報告は `tools/geap-probe/runs/abed744f-b8c1-4c54-a62f-cf9b2a5f4cfb/local-acceptance.json`。外部API呼出し0、実Check 0〜11はNOT_RUN。
- ブラウザ検証は第2節参照。中止後の状態表示／再試行と最新バックエンドの最終確認のみ未完了。

## 9. 再調査しなくてよい事項
- 着手時にリポジトリと祖先の適用AGENTS.mdなしを確認済み。
- 指定6ファイル（Few-shot仕様、同レビュー、Astra pipeline review、probe README、probe.mjs、validated_prompt）全文と既存実装を読了済み。現行方針はRound 18。
- Node v24.11.1、FFmpeg 4.4（`C:/ffmpeg-4.4/bin`）、pdfinfoあり。gcloudはPATH上になかった。
- MS-TCN採点参照の公式commitは `33ed91c0c7576650a2367efc602553af4c5295b1`。プローブREADMEに固定参照と採点説明あり。
- 実動画送信の同意・認証・プロジェクト・バケットが揃っていないため、実APIは試行していない。未確認事項を推測で埋めない。

## 10. 未コミット変更の概要
全実装は未コミット。既存の利用者資産を保全している。着手時点で `.gitignore` が変更済み、仕様／レビュー3文書、`docs/ui-proposal/`、`tools/` が未追跡だったため、未追跡を今回の生成物と一括判断しない。`reference/validated_prompt.md` と `docs/ui-proposal/` は変更していない。今回の主要変更は第5節。`data/`、`.local-validation/`、プローブrunsはローカル生成データで除外済み。コミット・push・外部公開・デプロイなし。

## 11. 再開時に最初に確認すべきファイル
1. 本書 `2026-09-07_HANDOFF.md`
2. `docs/FEWSHOT_IMPLEMENTATION.md`（未完了記述の最終更新が必要）
3. `.local-validation/final-tests.txt`（最終集計確認）
4. `docs/FEWSHOT_OPERATIONS.md` と `README.md`
5. `docs/2026-09-06_FEWSHOT_SPEC.md` の2.4・12.9など必要箇所
6. 変更箇所確認が必要なら `src/pipeline.js`、`src/cleanup.js`、`public/fewshot.js`、`tools/geap-probe/README.md`
