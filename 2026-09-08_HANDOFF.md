# Round 18 Few-shot TAS 引き継ぎ（2026-09-08）

## 結果

9月7日の最新引き継ぎ、git status / git diff、指定レビュー・仕様・運用・HTMLガイド、実装・テスト・画面を確認してレビュー8件を修正した。前回の未コミット変更は保全。コミット・push・デプロイなし。最新の作業記録は本書と `docs/FEWSHOT_IMPLEMENTATION.md` の9月8日節。前日の引き継ぎは履歴として残した。

## 今回の差分

- `.gitignore`: data-realと接続用合成入力を除外。
- `tools/prepare-connectivity-inputs.mjs`（新規）: 合成MP4を2本作成してlocalhostへ登録。語彙・識別条件・登録動画IDに一致する標準GTを出力。解析・外部API呼出し・モック結果移植はしない。
- `public/app.js`: 未反映フォームの状態管理、保存・選択変更前の検証／反映、不正入力保持、未保存表示・ページ離脱確認、追加区間ID。
- `public/fewshot.js`, `public/run-observer.js`（後者新規）: 再接続継続、完了後の回収確認、接続状態の独立表示、追加区間の語彙選択、具体的な警告、保存中の追加入力保全。
- `public/index.html`: 動画全体の確認事項、接続状態、確認済みJSONの説明。
- `src/pipeline.js`: 新規修正履歴に原本JSONを同梱する `original_result` の加算的拡張。旧履歴・AI原本は書き換えない。
- `test/run-observer.test.js`, `test/connectivity-inputs.test.js`（新規）、`test/fewshot.test.js`: 通信復帰、run切替、404、空の保存先への合成入力登録と後続モック完走、原本同梱・ハッシュ・追加区間の語彙検証。
- `README.md`, `docs/PRODUCTION_CONNECTIVITY_GUIDE.html`, `docs/FEWSHOT_OPERATIONS.md`, `docs/2026-09-06_FEWSHOT_SPEC.md`, `docs/FEWSHOT_IMPLEMENTATION.md`, `docs/2026-09-07_ROUND18_REVIEW.md`: 手順・JSON契約・保管・警告・検証結果を更新。

既存の他の未コミット／未追跡ファイルもそのまま残る。`reference/validated_prompt.md` と `docs/ui-proposal/` は未変更。Git上の差分全体には今回以前のRound 18実装が含まれ、今回だけの変更量ではない。

## 検証

- 全テスト39/39成功、失敗0。記録: `.local-validation/review-tests-final.txt`。
- プローブ自己検査成功。報告: `tools/geap-probe/runs/e5f81db5-d7b6-4561-bc7f-80efed2edc68/local-acceptance.json`。外部API呼出し0、実Check 0〜11はNOT_RUN。
- ブラウザ: モック標準作成・公開・分析、未反映表示→別区間選択で反映→履歴未保存、未反映から直接履歴保存、再読込して修正復元、不正時刻で選択変更を止め入力保持、区間追加→語彙選択→未反映から確認済みJSON出力、工程抜けと順序逆転の具体的な表示を確認。アプリとHTMLガイドの画面も目視確認。
- 合成入力コマンドを実行しローカル登録成功。実APIモードでの完走は未実施。統合テストの後続処理は明示的モック。
- `git diff --check`、JS構文検査成功。data-real内の動画・送信・生応答、connectivity-inputsのGTを `git check-ignore` で除外確認。
- 凍結プロンプトSHA-256: `06bfefc4e758e54bf8b4b54714fa727e4be68cc6176bf529402fce69a9ba8db5`（前回と一致）。

## 維持した性質・利用時の注意

GT隔離、原本と修正履歴の分離、Stage1共有条件、Stage2時刻不変、中止後の結果確定防止、実接続失敗のモック置換禁止を維持。新しい確認済みJSONは原本を同梱するが、動画バイトや生応答をまとめるアーカイブではない。旧履歴ファイルは原本JSONと組で保管する。警告は推論原本の判定として残し、修正で書き換えない。

通信失敗は4/8/16/30秒（以降30秒）間隔で再確認。サーバーの状態とは別表示。完了後の回収まで確認を継続し、対象runが404なら履歴から選び直す案内で停止する。

## 実測が必要な未確認事項

実APIのモデルID・リージョン・スキーマ・agentic互換、認証/IAM、GCS送信・削除・障害回収、ラベル精度・類似工程・安全網見逃し・人の確認量、18〜22分動画の時間／安定性、実使用量・費用。いずれも今回測定していない。3方式ともunverified。実接続へ進む際は修正済み接続ガイドを使用し、実行が完了し各runの一時媒体が削除完了、送信なしなら対象なしであることを確認する。

## ローカル検証データ

既存dataを使わず、検証用保存先 `.local-validation/review-fixes` を使用した。入力準備コマンドの生成物は `connectivity-inputs/`。いずれもGit除外対象。データの削除はしていない。


最終の画面確認では、案1 FewからZeroへの同一Stage1再判定が完了し、一時媒体「対象なし」、元の分析と標準作成は「削除完了」を確認。今回起動したモックサーバー（4173）とガイド表示専用サーバー（4180）は検証後に停止した。検証データは残している。
